-- ธงว่าแจ้งเตือนแถวนี้ถูกส่งอีเมลไปแล้วหรือยัง (หรือลองส่งแล้วแม้ไม่สำเร็จ)
-- ใช้โดย Edge Function "notify-email": pilot web เรียกฟังก์ชันนี้หลังทำ action สำเร็จ
-- (สร้างคำร้อง/อนุมัติ/มอบหมายช่าง/ตรวจรับ) โดยส่งแค่ request_id มา แล้วฟังก์ชันไปหา
-- แถวแจ้งเตือนของคำร้องนั้นที่ email_sent_at ยังว่างอยู่ ส่งอีเมลตาม employees.email
-- ของผู้รับ แล้วปั๊มเวลาไว้กันส่งซ้ำ — ไม่ต้องเดา "ใครควรได้รับ" ซ้ำอีกรอบฝั่ง client
-- เพราะ RPC ที่ insert แถวแจ้งเตือนคำนวณสิทธิ์/ผู้รับที่ถูกต้องไว้ให้แล้ว
alter table public.notifications
  add column if not exists email_sent_at timestamptz;
